#include "pages/include/topology_display.h"

void Topology_Display_Weight(smp_ctx_t *ctx)
{
    lv_obj_t *activity_content = ctx->pages[NAV_PAGE_TOPOLOGY]->activity_content;
    uint8_t flag_hidden = ctx->pages[NAV_PAGE_TOPOLOGY]->cur_page_is_hidden;

    
}
